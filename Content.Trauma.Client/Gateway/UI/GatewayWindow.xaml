<!--
SPDX-License-Identifier: AGPL-3.0-or-later
-->

<controls:FancyWindow xmlns="https://spacestation14.io"
                      xmlns:controls="using:Content.Client.UserInterface.Controls"
                      Title="{Loc 'gateway-window-title'}"
                      MinSize="800 360">
    <BoxContainer Orientation="Vertical">
        <BoxContainer Orientation="Horizontal">
            <Label Name="NextReadyLabel"
                   Text="{Loc 'gateway-window-portal-cooldown'}"
                   Margin="5"
                   SetWidth="128"/>
	        <ProgressBar Name="NextReadyBar"
	                     HorizontalExpand="True"
	                     MinValue="0"
	                     MaxValue="1"
	                     SetHeight="25"/>
	        <Label Name="NextCloseText" Text="0" Margin="5"/>
	    </BoxContainer>
	    <controls:Separator ColorOverride="#404040" Margin="0 5 0 5"/>
	    <BoxContainer Name="Container" Orientation="Vertical" Margin="5 0 5 0"/>
    </BoxContainer>
</controls:FancyWindow>
